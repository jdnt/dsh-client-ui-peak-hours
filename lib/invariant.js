//#region lib/types/invariant.js
/** Invariant companion plugin (no assertions — nothing to check at runtime). */
/** Provides no assertions: the peak-hours plugin owns no cross-package runtime invariants. */
function apply() {}
//#endregion
export { apply };
